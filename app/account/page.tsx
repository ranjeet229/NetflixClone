import UploadFormFields from "@/components/uploads/upload-form-field";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <UploadFormFields />
    </div>
  );
};

export default page;
